/**
   Copyright (c) 2025 impact soft

*/

/**
 * @file sample_game.ino
 * @brief Arduino sketch entry point for the sample game.
 * @ingroup sample_app
 *
 * Carries no code of its own. The Arduino IDE compiles the sketch directory, so
 * including system.h here is what pulls the whole firmware into the build.
 *
 * The Arduino entry points themselves -- `setup()`, `loop()`, `setup1()` and
 * `loop1()` -- come from `sys/rp_core0.h` and `sys/rp_core1.h`, included last by
 * system.h.
 *
 * @see @ref sample_game, @ref dev_setup
 */

#include "system.h"

